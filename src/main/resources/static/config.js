// Configuración del frontend estático (GitHub Pages)
// backendBase: si lo definís, los formularios se enviarán a ese backend desplegado
// Ejemplo: window.APP_CONFIG = { backendBase: 'https://tu-backend.ejemplo.com' };
window.APP_CONFIG = window.APP_CONFIG || {
  backendBase: null
};
